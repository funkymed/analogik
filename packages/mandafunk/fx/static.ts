/**
 * @module @mandarine/mandafunk/fx/static
 *
 * Re-exports StaticItems from the core module for backward compatibility.
 * Prefer importing from `@mandarine/mandafunk/core` or `@mandarine/mandafunk` directly.
 *
 * @deprecated Import from `../core/StaticItems` instead.
 */
export { StaticItems } from "../core/StaticItems";
